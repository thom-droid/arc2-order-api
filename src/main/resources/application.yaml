spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/indexlab
    username: labuser
    password: labpass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true